#include <bits/stdc++.h>
using namespace std;

// Q.1 why we are using priority queue instead of queue
// simple queue is storing distance in queue and dist array which are not shortest so loop will run for many times
// while in pririty queue take min distance first so we most of the times get shortest dist
// so pq will not store intermediate or unnecesory distances so reduce the loop time

// Q.2 Tc ?
// O(E * log(V))

int main(){
    
    return 0;
}